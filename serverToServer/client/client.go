package client

import (
/*"github.com/majestrate/i2p-tools/sam3"
"github.com/i2pfs/i2pfsd/misc"
"github.com/i2pfs/i2pfsd/log"*/
)

func Connect(samUrl, peersFilePath string) {
	/*log.Debugln("Client: NewSAM")
	sam, err := sam3.NewSAM(samUrl)
	misc.CheckError(err)
	log.Debugln("Client: NewKeys")
	keys, err := sam.NewKeys()
	misc.CheckError(err)
	log.Debugln("Client: NewStreamSession")
	stream, err := sam.NewStreamSession("clientTun1", keys, sam3.Options_Small)
	misc.CheckError(err)
	log.Debugln("Client: Connecting to " + server.Base32())
	conn, err := stream.DialI2P(server)
	misc.CheckError(err)
	log.Debugln("Client: Write")
	conn.Write([]byte("Hello world!"))*/
}
